set timeout=3
set default=0

menuentry "GeckoOS (64-bit)" {
    set gfxmode=auto
    set gfxpayload=keep
    multiboot2 /boot/kernel.elf
    boot
}

menuentry "GeckoOS (64-bit, serial debug)" {
    set gfxmode=auto
    set gfxpayload=keep
    multiboot2 /boot/kernel.elf geckoOS.debug=serial
    boot
}